---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

{{ if .HasExample -}}
## Example Usage

{{ tffile .ExampleFile }}
{{- end }}

## Container padding

The container takes a padding on every side, a padding per axis, or both. An axis value wins over
`padding`, and `padding` wins over the platform default of 64px.

Terraform owns all three. A value you configure is written, `0` included, and an axis you leave out
of the configuration clears whatever override is stored against the tenant on the next apply.

### One padding on every side

{{ tffile "examples/resources/authsignal_theme/padding-universal.tf" }}

### A padding per axis

{{ tffile "examples/resources/authsignal_theme/padding-both-axes.tf" }}

### A universal padding with one axis overridden

{{ tffile "examples/resources/authsignal_theme/padding-universal-plus-one-axis.tf" }}

{{ .SchemaMarkdown | trimspace }}
{{- if .HasImport }}

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

{{ codefile "shell" .ImportFile }}
{{- end }}
