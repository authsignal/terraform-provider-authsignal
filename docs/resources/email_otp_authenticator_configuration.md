---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "authsignal_email_otp_authenticator_configuration Resource - terraform-provider-authsignal"
subcategory: ""
description: |-
  Manages the tenant's email OTP authenticator configuration. Destroying the resource deletes the configuration.
---

# authsignal_email_otp_authenticator_configuration (Resource)

Manages the tenant's email OTP authenticator configuration. Destroying the resource deletes the configuration.

## Example Usage

```terraform
variable "sendgrid_api_key" {
  type      = string
  ephemeral = true
  sensitive = true
}

resource "authsignal_email_otp_authenticator_configuration" "email_otp" {
  email_provider = "SENDGRID"

  sendgrid_email_credentials = {
    api_key     = var.sendgrid_api_key
    template_id = "d-00000000000000000000000000000000"
    from_email  = "noreply@example.com"
    from_name   = "Example"
  }

  sendgrid_email_credentials_version = "1"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `email_provider` (String) Email delivery provider. Supply its credential block when first selecting it. One of `BIRD`, `MAILGUN`, `MAILJET`, `MANDRILL`, `SENDGRID`, `SMTP`.

### Optional

- `allowed_custom_email_variables` (Set of String) Custom email template variables. Set to `[]` to clear them.
- `bird_email_credentials` (Attributes) Bird email credentials. Required when `email_provider` is `BIRD` and no Bird credentials are stored yet. (see [below for nested schema](#nestedatt--bird_email_credentials))
- `bird_email_credentials_version` (String) Credential rotation marker for `bird_email_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `is_active` (Boolean)
- `mailgun_email_credentials` (Attributes) Mailgun credentials. Required when `email_provider` is `MAILGUN` and no Mailgun credentials are stored yet. (see [below for nested schema](#nestedatt--mailgun_email_credentials))
- `mailgun_email_credentials_version` (String) Credential rotation marker for `mailgun_email_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `mailjet_email_credentials` (Attributes) Mailjet credentials. Required when `email_provider` is `MAILJET` and no Mailjet credentials are stored yet. (see [below for nested schema](#nestedatt--mailjet_email_credentials))
- `mailjet_email_credentials_version` (String) Credential rotation marker for `mailjet_email_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `mandrill_email_credentials` (Attributes) Mandrill credentials. Required when `email_provider` is `MANDRILL` and no Mandrill credentials are stored yet. (see [below for nested schema](#nestedatt--mandrill_email_credentials))
- `mandrill_email_credentials_version` (String) Credential rotation marker for `mandrill_email_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `sendgrid_email_credentials` (Attributes) SendGrid credentials. Required when `email_provider` is `SENDGRID` and no SendGrid credentials are stored yet. (see [below for nested schema](#nestedatt--sendgrid_email_credentials))
- `sendgrid_email_credentials_version` (String) Credential rotation marker for `sendgrid_email_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `sending_rate_limit_configurations` (Attributes List) Email delivery limits. All configured limits apply. (see [below for nested schema](#nestedatt--sending_rate_limit_configurations))
- `smtp_email_credentials` (Attributes) SMTP credentials. Required when `email_provider` is `SMTP` and no SMTP credentials are stored yet. (see [below for nested schema](#nestedatt--smtp_email_credentials))
- `smtp_email_credentials_version` (String) Credential rotation marker for `smtp_email_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `submission_rate_limit_configuration` (Attributes) Code submission limit. (see [below for nested schema](#nestedatt--submission_rate_limit_configuration))

### Read-Only

- `authenticator_id` (String)
- `verification_method` (String)

<a id="nestedatt--bird_email_credentials"></a>
### Nested Schema for `bird_email_credentials`

Required:

- `channel_id` (String) Bird channel ID.
- `project_id` (String) Bird project ID.
- `workspace_id` (String) Bird workspace ID.

Optional:

- `access_key` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Bird access key. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.
- `locale` (String) The locale Bird renders message templates in.
- `sender_email` (String) Address the message is sent from.
- `sender_name` (String) Display name the message is sent from.
- `version_id` (String) Bird template version ID. It is unrelated to `bird_email_credentials_version`, the credential rotation marker.


<a id="nestedatt--mailgun_email_credentials"></a>
### Nested Schema for `mailgun_email_credentials`

Required:

- `domain` (String) Mailgun sending domain.
- `template_name` (String) Mailgun template name.
- `url` (String) The Mailgun API base URL for the region the domain is in.

Optional:

- `api_key` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Mailgun API key. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.
- `from` (String) Address the message is sent from.


<a id="nestedatt--mailjet_email_credentials"></a>
### Nested Schema for `mailjet_email_credentials`

Required:

- `public_key` (String) Mailjet API key.
- `template_id` (Number) Mailjet template ID.

Optional:

- `private_key` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Mailjet API secret key. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.


<a id="nestedatt--mandrill_email_credentials"></a>
### Nested Schema for `mandrill_email_credentials`

Required:

- `template_name` (String) Mandrill template name.

Optional:

- `api_key` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Mandrill API key. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.


<a id="nestedatt--sendgrid_email_credentials"></a>
### Nested Schema for `sendgrid_email_credentials`

Required:

- `from_email` (String) Address the message is sent from.
- `template_id` (String) SendGrid dynamic template ID.

Optional:

- `api_key` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) SendGrid API key. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.
- `from_name` (String) Display name the message is sent from.


<a id="nestedatt--sending_rate_limit_configurations"></a>
### Nested Schema for `sending_rate_limit_configurations`

Required:

- `rate_limit` (Number) Maximum attempts in the window. Must be between 1 and 100.
- `window_in_minutes` (Number) Window length in minutes. Must be between 1 and 1440; fractional values are supported.


<a id="nestedatt--smtp_email_credentials"></a>
### Nested Schema for `smtp_email_credentials`

Required:

- `from` (String) Address the message is sent from.
- `host` (String) SMTP server hostname.
- `port` (Number) SMTP server port.
- `secure` (Boolean) Whether the connection uses TLS.
- `user` (String) SMTP username.

Optional:

- `from_name` (String) Display name the message is sent from.
- `password` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) SMTP password. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.
- `reply_to` (String) Address replies are directed to.
- `reply_to_name` (String) Display name replies are directed to.


<a id="nestedatt--submission_rate_limit_configuration"></a>
### Nested Schema for `submission_rate_limit_configuration`

Required:

- `rate_limit` (Number) Maximum attempts in the window. Must be between 1 and 100.
- `window_in_minutes` (Number) Window length in minutes. Must be between 1 and 1440; fractional values are supported.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import authsignal_email_otp_authenticator_configuration.email_otp email-otp
```
