---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "authsignal_sms_authenticator_configuration Resource - terraform-provider-authsignal"
subcategory: ""
description: |-
  Manages the tenant's SMS OTP authenticator configuration. Destroying the resource deletes the configuration.
---

# authsignal_sms_authenticator_configuration (Resource)

Manages the tenant's SMS OTP authenticator configuration. Destroying the resource deletes the configuration.

## Example Usage

```terraform
variable "twilio_auth_token" {
  type      = string
  ephemeral = true
  sensitive = true
}

resource "authsignal_sms_authenticator_configuration" "sms" {
  sms_provider         = "TWILIO"
  sms_country_codes    = ["AU", "NZ"]
  default_country_code = "NZ"

  twilio_credentials = {
    auth_token            = var.twilio_auth_token
    messaging_service_sid = "MG00000000000000000000000000000000"
    account_sid           = "AC00000000000000000000000000000000"
  }

  twilio_credentials_version = "1"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `sms_provider` (String) SMS delivery provider. Supply its credential block when first selecting it. One of `BIRD`, `MESSAGE_MEDIA`, `MODICA_GROUP`, `TNZ`, `TWILIO`, `WEBHOOK`.

### Optional

> **NOTE**: [Write-only arguments](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments) are supported in Terraform 1.11 and later.

- `allowed_custom_sms_variables` (Set of String) Custom SMS template variables. Set to `[]` to clear them.
- `bird_sms_credentials` (Attributes) Bird SMS credentials. Required when `sms_provider` is `BIRD` and no Bird credentials are stored yet. (see [below for nested schema](#nestedatt--bird_sms_credentials))
- `bird_sms_credentials_version` (String) Credential rotation marker for `bird_sms_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `default_country_code` (String) Country code used for numbers without one. It must be included in `sms_country_codes`.
- `is_active` (Boolean)
- `message_media_credentials` (Attributes) MessageMedia credentials. Required when `sms_provider` is `MESSAGE_MEDIA` and no MessageMedia credentials are stored yet. (see [below for nested schema](#nestedatt--message_media_credentials))
- `message_media_credentials_version` (String) Credential rotation marker for `message_media_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `modica_group_credentials` (Attributes) Modica Group credentials. Required when `sms_provider` is `MODICA_GROUP` and no Modica Group credentials are stored yet. (see [below for nested schema](#nestedatt--modica_group_credentials))
- `modica_group_credentials_version` (String) Credential rotation marker for `modica_group_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `prefix_rate_limit_configurations` (Attributes List) Message delivery limits by number prefix. Set to `[]` to clear them. (see [below for nested schema](#nestedatt--prefix_rate_limit_configurations))
- `sending_rate_limit_configurations` (Attributes List) Message delivery limits. All configured limits apply. (see [below for nested schema](#nestedatt--sending_rate_limit_configurations))
- `sms_country_codes` (Set of String) Allowed two-letter destination country codes. Set to `[]` to clear the restriction and `default_country_code`.
- `submission_rate_limit_configuration` (Attributes) Code submission limit. (see [below for nested schema](#nestedatt--submission_rate_limit_configuration))
- `tnz_credentials` (Attributes, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) TNZ credentials. Required when `sms_provider` is `TNZ` and no TNZ credentials are stored. This block is entirely write-only, so `tnz_credentials_version` is required when it is set. (see [below for nested schema](#nestedatt--tnz_credentials))
- `tnz_credentials_version` (String) Credential rotation marker for `tnz_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `twilio_credentials` (Attributes) Twilio credentials. Required when `sms_provider` is `TWILIO` and no Twilio credentials are stored yet. (see [below for nested schema](#nestedatt--twilio_credentials))
- `twilio_credentials_version` (String) Credential rotation marker for `twilio_credentials`. Change it to resend the credentials. Use an opaque counter or date, never a secret.
- `webhook_url` (String) HTTPS endpoint for verification codes. Required for `WEBHOOK`; omit for other providers. Changing providers clears it.

### Read-Only

- `authenticator_id` (String)
- `verification_method` (String)

<a id="nestedatt--bird_sms_credentials"></a>
### Nested Schema for `bird_sms_credentials`

Required:

- `workspace_id` (String) Bird workspace ID.

Optional:

- `access_key` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Bird access key. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.
- `channel_id` (String) Bird channel ID.
- `enable_message_templates` (Boolean) Whether Bird message templates are used instead of the message body Authsignal sends.
- `locale` (String) The locale Bird renders message templates in.
- `navigator_id` (String) Bird navigator ID.
- `project_id` (String) Bird project ID.


<a id="nestedatt--message_media_credentials"></a>
### Nested Schema for `message_media_credentials`

Optional:

- `api_key` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) MessageMedia API key. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.
- `api_secret` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) MessageMedia API secret. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.
- `source_number` (String) Number messages are sent from.


<a id="nestedatt--modica_group_credentials"></a>
### Nested Schema for `modica_group_credentials`

Required:

- `username` (String) Modica Group username.

Optional:

- `password` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Modica Group password. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.


<a id="nestedatt--prefix_rate_limit_configurations"></a>
### Nested Schema for `prefix_rate_limit_configurations`

Optional:

- `block_size` (String) How numbers are grouped: the string `country`, or a positive number of leading digits.
- `country_codes` (Set of String) The two-letter country codes this limit applies to.
- `enabled` (Boolean)
- `rate_limit` (Number) Maximum messages in the window. Must be between 1 and 1000.
- `window_in_minutes` (Number) Window length in minutes. Must be greater than 0 and at most 1440; fractional values are supported.


<a id="nestedatt--sending_rate_limit_configurations"></a>
### Nested Schema for `sending_rate_limit_configurations`

Required:

- `rate_limit` (Number) Maximum attempts in the window. Must be between 1 and 100.
- `window_in_minutes` (Number) Window length in minutes. Must be between 1 and 1440; fractional values are supported.


<a id="nestedatt--submission_rate_limit_configuration"></a>
### Nested Schema for `submission_rate_limit_configuration`

Required:

- `rate_limit` (Number) Maximum attempts in the window. Must be between 1 and 100.
- `window_in_minutes` (Number) Window length in minutes. Must be between 1 and 1440; fractional values are supported.


<a id="nestedatt--tnz_credentials"></a>
### Nested Schema for `tnz_credentials`

Optional:

- `api_key` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) TNZ API key. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.


<a id="nestedatt--twilio_credentials"></a>
### Nested Schema for `twilio_credentials`

Required:

- `account_sid` (String) Twilio Account SID.
- `messaging_service_sid` (String) Twilio Messaging Service SID.

Optional:

- `auth_token` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Twilio auth token. Requires Terraform 1.11 or later; supply it through an ephemeral, sensitive variable.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import authsignal_sms_authenticator_configuration.sms sms
```
